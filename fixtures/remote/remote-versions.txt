$ bluetoothctl --version
bluetoothctl: 5.66

$ pipewire --version
pipewire
Compiled with libpipewire 1.2.7
Linked with libpipewire 1.2.7

$ wireplumber --version
wireplumber
Compiled with libwireplumber 0.4.13
Linked with libwireplumber 0.4.13

$ pulseaudio --version
pulseaudio 16.1
(NOTE: installed but NOT the active server; pipewire-pulse provides the PulseAudio
 API/socket. systemctl --user shows pipewire/wireplumber/pipewire-pulse active,
 pulseaudio.service inactive. pgrep shows pipewire, wireplumber, pipewire-pulse
 running; no standalone pulseaudio daemon.)
